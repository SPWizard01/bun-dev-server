<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>

  <body>
    <a href="../">..</a>
    <% dirs.forEach(element => { %> <a href=".<%= element.requestPath %>/<%= element.name %>"><%= element.name %></a> <% }) %>
    <% files.forEach(element => { %> <a href=".<%= element.requestPath %>/<%= element.name %>"><%= element.name %></a> <% }) %>
  </body>

</html>